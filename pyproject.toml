[project]
name = "jobkit"
version = "0.1.0"
description = "Resume build + job-application workspace tooling"
readme = "README.md"
requires-python = ">=3.12,<3.14"
dependencies = [
  "weasyprint>=63",
  "pypandoc-binary>=1.13",
  "beautifulsoup4>=4.12",
  "httpx>=0.28",
]

[dependency-groups]
dev = [
  "dslop>=0.2.2",
  "ruff>=0.15.13",
  "ty>=0.0.35",
]

[project.scripts]
build-resume = "jobkit.resume.build:main"
fetch-jobs = "jobkit.jobs.cli:main"
outreach = "jobkit.outreach.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/jobkit"]

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["src"]

[tool.ty.environment]
root = ["./src"]

[tool.ruff.lint]
select = ["ALL"]
ignore = [
    # Ruff formatter compatibility:
    # https://docs.astral.sh/ruff/formatter/#conflicting-lint-rules
    "W191",
    "E111",
    "E114",
    "E117",
    "D203",
    "D206",
    "D300",
    "Q000",
    "Q001",
    "Q002",
    "Q003",
    "Q004",
    "COM812",
    "COM819",
    "ISC002",
    # pydocstyle alternative convention conflict; keep D212.
    "D213",
]

[tool.ruff.lint.per-file-ignores]
"src/jobkit/resume/build.py" = ["T201"]
"src/jobkit/jobs/cli.py" = ["T201"]
"src/jobkit/outreach/cli.py" = ["T201"]
